meta {
  name: Grant User Role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/{{userId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "roleId": "{{roleId}}"
  }
}
