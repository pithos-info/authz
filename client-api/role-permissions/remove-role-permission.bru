meta {
  name: Remove Role Permission
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/roles/{{roleId}}/permissions/{{permission}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
