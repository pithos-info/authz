meta {
  name: Update Role
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/roles/{{roleId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "editor",
    "description": "Can read, write, and publish content"
  }
}
