meta {
  name: Create Role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "editor",
    "description": "Can read and write content"
  }
}
