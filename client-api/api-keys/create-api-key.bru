meta {
  name: Create API Key
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/apikeys
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "my-api-key",
    "description": "Used by CI pipeline"
  }
}
