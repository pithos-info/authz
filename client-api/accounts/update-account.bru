meta {
  name: Update Account
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/accounts/{{accountId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Acme Corp Updated"
  }
}
